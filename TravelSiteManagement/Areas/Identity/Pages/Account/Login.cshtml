@page
@model LoginModel

@{
    ViewData["Title"] = "Log in";
}
<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/Login.css">
</head>

<body>
   
   <div class="row">
       <div class="col-md-4">
           <section>
               <form id="account" method="post">
                    <h1>@ViewData["Title"]</h1>
                   <h2>Already CheapTravel client</h2>
                   <hr/>
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                    <div class="mb-3">
                        <div class="input-container">
                            <img id="login-icon" src="~/Assets/Login-Icon.jpg" />
                            <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                        </div>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <div class="input-container">
                            <img id="password-icon" src="~/Assets/Password-Icon.png" />
                            <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                        </div>
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>
                   <div class="checkbox mb-3">
                       <label asp-for="Input.RememberMe" class="form-label">
                           <input class="form-check-input" asp-for="Input.RememberMe" />
                           @Html.DisplayNameFor(m => m.Input.RememberMe)
                       </label>
                   </div>
                   <div>
                       <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary">Log in</button>
                   </div>
                   
                   <div>
                       <p>
                           <a id="forgot-password" asp-page="./ForgotPassword">Forgot your password?</a>
                       </p>
                   </div>
                   @*
                       <p>
                           <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Register as a new user</a>
                       </p>
                       <p>
                           <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Resend email confirmation</a>
                       </p>
                   </div>
                   *@
               </form>
           </section>
       </div>
       <div class="col-md-6 col-md-offset-2">
          <h1>Don't have an account? Register now!</h1>
          <h2>Benefits for logged users:</h2>
          <ul>
                <li>Exclusive discounts on tours and vacation packages.</li>
                <li>Early access to new tours before they are available to others.</li>
                <li>Personalized tour offers based on past bookings and preferences.</li>
                <li>Booking history available in the user’s profile.</li>
                <li>Free travel insurance or the option to purchase it at a discounted rate.</li>
                <li>Discounted extra luggage or transfer services.</li>
            </ul>
            <form id="registerProvider" asp-area="Identity" asp-page="/Account/Register">
                <button id="register-button">Register</button>
            </form>
       </div>
   </div>

   @section Scripts {
       <partial name="_ValidationScriptsPartial" />
   }
</body>
<html/>